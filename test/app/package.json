{
  "private": "true",
  "name": "@goosewobbler/spectron-e2e-app",
  "version": "1.0.0",
  "main": "./dist/main.js",
  "scripts": {
    "build": "pnpm run clean:dist && webpack && cross-env cp ./src/index.html ./dist && electron-builder",
    "clean": "cross-env rm -rf ./node_modules && pnpm run clean:dist",
    "clean:dist": "cross-env rm -rf ./dist && cross-env mkdir -p ./dist"
  },
  "devDependencies": {
    "electron": "^15.3.0",
    "electron-builder": "^22.13.1",
    "webpack": "^5.60.0",
    "webpack-cli": "^4.9.1"
  },
  "engines": {
    "node": ">=14.17.1"
  },
  "build": {
    "asar": true,
    "appId": "com.spectron-e2e.demo",
    "copyright": "goosewobbler",
    "productName": "spectron-e2e",
    "files": [
      "./dist/*"
    ],
    "linux": {
      "executableName": "spectron-e2e",
      "category": "Utility",
      "target": [
        "AppImage"
      ]
    }
  }
}
